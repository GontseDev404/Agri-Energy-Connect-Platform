@model ST10038937_prog7311_poe1.Models.Farmer

@{
    ViewData["Title"] = Localizer["Delete Farmer"];
}

<h1 data-translate-key="Delete Farmer">Delete Farmer</h1>

<h3 data-translate-key="Are you sure you want to delete this farmer?">Are you sure you want to delete this farmer?</h3>
<div>
    <h4 data-translate-key="Farmer">Farmer</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2" data-translate-key="Name">Name</dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class="col-sm-2" data-translate-key="Farm Name">Farm Name</dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.FarmName)
        </dd>
        <dt class="col-sm-2" data-translate-key="Email">Email</dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Email)
        </dd>
        <dt class="col-sm-2" data-translate-key="Phone">Phone</dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.PhoneNumber)
        </dd>
        <dt class="col-sm-2" data-translate-key="Address">Address</dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Address)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="FarmerId" />
        <input type="submit" value="@Localizer["Delete"]" class="btn btn-danger" /> |
        <a asp-action="Index" data-translate-key="Back to List">Back to List</a>
    </form>
</div>
